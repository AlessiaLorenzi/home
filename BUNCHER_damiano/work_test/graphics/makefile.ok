#
# README: Portions of this file are merged at file generation
# time. Edits can be made *only* in between specified code blocks, look
# for keywords <Begin user code> and <End user code>.
#
#
# Created by Builder Xcessory 4.0
# Generated by Code Generator Xcessory 2.0 (09/09/96)
#
#

# Begin user code block <make_user_files>
      USER_SRCS =
      USER_OBJS =
    USER_CFLAGS =
 USER_LIBRARIES =
# End user code block <make_user_files>

             RM = rm -f
             CC = cc
         CFLAGS = $(USER_CFLAGS) -I${OPENWINHOME}/include
           LIBS = $(USER_LIBRARIES) -L${OPENWINHOME}/lib -lXm -lXt -lX11

OFILES = $(USER_OBJS) \
         main-c.o \
         creation-c.o \
         callbacks-c.o \
         bxutils-c.o

FILES = $(USER_SRCS) \
        main-c.c \
        creation-c.c \
        callbacks-c.c \
        bxutils-c.c

# User-specified options:


all: main-c

main-c: $(OFILES)
	$(CC) $(CFLAGS) $(OFILES) $(LIBS) -o $@

ocodecenter:: 
	#load $(CFLAGS) $(OFILES) $(LIBS)

codecenter:: 
	#load $(CFLAGS) $(FILES) $(LIBS)

clean::
	$(RM) $(OFILES) main-c
